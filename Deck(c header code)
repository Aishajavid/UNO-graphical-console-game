#pragma once
#include "Card.h"
#include <vector>
#include <algorithm>
#include <random>
#include <chrono>

class Deck {
public:
    std::vector<Card> cards;

    Deck();
    void shuffle();
    Card draw();
    bool isEmpty() const { return cards.empty(); }
    int  size()    const { return (int)cards.size(); }

    // Rebuild deck from discard pile when empty
    void refillFrom(std::vector<Card>& discardPile);
};
